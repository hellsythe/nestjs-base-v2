#!/usr/bin/env bash

YELLOW='\033[1;33m'
GREEN='\033[0;32m'
NC='\033[0m'

STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM)

# TESTING_CHANGED=false
# ARCHITECTURE_CHANGED=false

# if echo "$STAGED_FILES" | grep -E "\.spec\.ts$|\.feature$|^test/" >/dev/null; then
#   TESTING_CHANGED=true
# fi

# if echo "$STAGED_FILES" | grep -E "^src/[^/]+/[^/]+\.module\.ts$" >/dev/null; then
#   ARCHITECTURE_CHANGED=true
# fi

SHOW_REMINDER=false

# if [ "$TESTING_CHANGED" = true ]; then
#   echo -e "${YELLOW}⚠️  REMINDER: You changed tests${NC}"
#   echo -e "   Consider updating: ${GREEN}docs/Testing.md${NC}"
#   SHOW_REMINDER=true
# fi

# if [ "$ARCHITECTURE_CHANGED" = true ]; then
#   echo -e "${YELLOW}⚠️  REMINDER: You changed module architecture${NC}"
#   echo -e "   Consider updating: ${GREEN}docs/Architecture.md${NC}"
#   SHOW_REMINDER=true
# fi

if [ "$SHOW_REMINDER" = true ]; then
  echo -e "${YELLOW}Press Enter to continue commit or Ctrl+C to cancel.${NC}"
  read -r
fi

exit 0
